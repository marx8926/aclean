= form_for(resource, :as => resource_name, :html=>{:class=>'login-wrapper'}, :url => session_path(resource_name)) do |f|
  .header
    .row
      .col-md-12
        %h3
          Login
          = image_tag("logo.png", :class => 'pull-right', :alt => 'Logo')
        %p Fill out the form below to login.
  .content
    .row
      .col-md-12.col-sm-12
        %br/
        = f.email_field :email, :autofocus => true, :class => 'input col-md-12 col-sm-12 email', :placeholder => 'example@mailll.com'
    .row
      .col-md-12.col-sm-12
        %br/
        = f.password_field :password, :class => 'input col-md-12 col-sm-12 password', :placeholder => '***********'

 
  .actions
    %div= f.submit "Sign in", :class => 'btn btn-danger'
    = render "devise/shared/links"
